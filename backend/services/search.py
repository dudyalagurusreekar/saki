def multi_search(query: str):
    """
    Placeholder search logic
    Replace later with real API (SerpAPI, DuckDuckGo, etc.)
    """
    return [
        f"Top result for '{query}'",
        f"More info about '{query}'",
        f"Latest updates on '{query}'"
    ]